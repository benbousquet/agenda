import React from "react"

function Event(props){
    return(
        <React.Fragment>
            <html shit>{props.date}</html>
            <html shit>{props.eventName}</html>
        </React.Fragment>
    )
}
export default Event